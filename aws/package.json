{
  "name": "hufs-lost-and-found-aws",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "sandbox": "ampx sandbox",
    "deploy": "ampx pipeline-deploy --branch ${AWS_BRANCH:-main} --app-id ${AWS_AMPLIFY_APP_ID:-$AWS_APP_ID} --outputs-out-dir src"
  },
  "dependencies": {
    "@aws-amplify/ui-react": "6.15.3",
    "aws-amplify": "6.17.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@aws-amplify/backend": "1.22.0",
    "@aws-amplify/backend-cli": "1.8.2",
    "@types/react": "18.3.28",
    "@types/react-dom": "18.3.7",
    "@vitejs/plugin-react": "6.0.1",
    "esbuild": "^0.27.0",
    "typescript": "5.9.3",
    "vite": "8.0.11",
    "vitest": "4.1.5"
  }
}
